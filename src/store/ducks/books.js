// Actions Types
export const Types = {};

// Reducer
const initialState = {};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

// Action creators

export default reducer;
